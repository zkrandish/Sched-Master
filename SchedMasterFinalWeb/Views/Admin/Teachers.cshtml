@model IEnumerable < SchedMasterFinalWeb.Models.User>

@{
    ViewBag.Title = "Students";
}

<link rel="stylesheet" type="text/css" href="~/Content/AdminStyles/AdminUser.css">

<div class="centered-container">

    <!-- Search bar -->
    <div class="search-bar">
        <input type="text" placeholder="Search..." />
        <button type="submit">Search</button>
    </div>

    <p>
        @Html.ActionLink("Create New", "AddForm", "AdminTeachers", null, new { @class = "btn-create" })
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfBirth)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EmailAddress)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateOfBirth)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PhoneNumber)
                </td>
                <td>
                    @Html.ActionLink("Edit", "UpdateForm", "AdminTeachers", new { id = item.UserId }, new { @class = "action-button" })
                    @Html.ActionLink("Delete", "DeleteConfirmed", "AdminTeachers", new { id = item.UserId }, new { @class = "action-button delete-button" })

                    @*<a href="javascript:void(0);"
                           onclick="confirmDeletion(@item.UserId);"
                           class="action-button delete-button">
                            Delete
                        </a>*@
                </td>
            </tr>
        }

    </table>

</div>

@*@if (ViewBag.deleteFlag == true)
    {
        <script type="text/javascript">
            alert('@ViewBag.Message');
        </script>
    }*@


@if (!string.IsNullOrEmpty(ViewBag.Message))
{
    <script type="text/javascript">
    alert('@ViewBag.Message');
    </script>
}

@*<script type="text/javascript">
        function confirmDeletion(id) {
            var result = confirm("Are you sure you want to delete Student: " + id + "?");
            if (result) {
                // If the user clicked "OK", proceed with the deletion using an AJAX POST request
                RedirectToAction("DeleteConfirmed", "AdminStudents", id);
            }
        }
    </script>*@


